# SQLite Database URL (Padrão)
DATABASE_URL="file:./db/custom.db"

# Para usar um banco de dados externo (ex: Supabase/PostgreSQL), 
# você deve alterar o provider no prisma/schema.prisma para "postgresql"
# e rodar npx prisma generate novamente.
# DATABASE_URL="postgres://user:password@host:port/database"
